<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Custom permission to secure the content provider -->
    <permission
        android:name="com.iips.launcher.permission.READ_SHARED_CONFIG"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.iips.launcher.permission.READ_SHARED_CONFIG" />

    <!-- Required permissions for Device Owner -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" 
        tools:ignore="QueryAllPackagesPermission" />
        
    <!-- MDM Telemetry Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" 
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.GET_PACKAGE_SIZE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <!-- System App Permissions -->


    <!--
        System App Configuration:
        To install as a system app, uncomment android:sharedUserId below and install to /system/priv-app/
        This requires root access and signing with platform keys (or test keys for development)
    -->
    <application
        android:name=".LauncherApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dotroid"
        android:testOnly="true"
        tools:targetApi="31">
        
        <!-- Main Launcher Activity - Set as Home Screen -->
        <activity
            android:name=".ui.LauncherActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.App.Starting"
            android:excludeFromRecents="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Admin Panel Activity -->
        <activity
            android:name=".ui.AdminActivity"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Dotroid.NoActionBar"
            android:excludeFromRecents="true"
            android:exported="false" />

        <!-- Device Admin Receiver (DPC) -->
        <!-- ACTION_PROFILE_PROVISIONING_COMPLETE is required for QR / zero-touch enrollment -->
        <receiver
            android:name=".policy.DeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.PROFILE_PROVISIONING_COMPLETE" />
                <action android:name="android.app.action.PROVISIONING_COMPLETE" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver - Auto-start on boot (System App Feature) -->
        <receiver
            android:name=".core.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- Onboarding Activity -->
        <activity
            android:name=".ui.OnboardingActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Dotroid.NoActionBar"
            android:excludeFromRecents="true"
            android:exported="false" />

        <!-- QR Code Scanner Activity -->
        <activity
            android:name=".ui.QrScannerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Dotroid.NoActionBar"
            android:excludeFromRecents="true"
            android:exported="false" />

        <!-- Pending Activity -->
        <activity
            android:name=".ui.PendingActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Dotroid.NoActionBar"
            android:excludeFromRecents="true"
            android:exported="false" />

        <!-- App Selection Activity -->
        <activity
            android:name=".ui.AppSelectionActivity"
            android:label="@string/select_apps"
            android:theme="@style/Theme.Dotroid.NoActionBar"
            android:exported="false" />

        <service
            android:name=".policy.GeofenceService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <service
            android:name=".guard.service.GuardLocationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <activity
            android:name=".ui.GeofenceEnrollmentActivity"
            android:label="Geofence Enrollment"
            android:theme="@style/Theme.Dotroid.NoActionBar"
            android:exported="false" />

        <!-- Enterprise Provisioning Bootstrap Service -->
        <!-- Started automatically by DeviceAdminReceiver after QR provisioning -->
        <service
            android:name=".provisioning.ProvisioningBootstrapService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice"
            tools:ignore="ForegroundServiceType" />

        <!-- Provisioning Status UI (enterprise QR path only) -->
        <activity
            android:name=".ui.ProvisioningStatusActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Dotroid.NoActionBar"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTop" />

        <service
            android:name=".network.MdmSocketService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:ignore="ForegroundServiceType" />

        <service
            android:name=".pocket.service.ApkDirectoryObserverService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:ignore="ForegroundServiceType" />

        <service
            android:name=".pocket.service.AppHealthMonitoringService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:ignore="ForegroundServiceType" />

        <receiver
            android:name=".apps.monitoring.PackageMonitorReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_CHANGED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".install.SilentInstallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.iips.launcher.INSTALL_COMPLETE" />
            </intent-filter>
        </receiver>

        <!-- General Storage Provider for Managed Apps -->
        <provider
            android:name=".provider.SharedConfigProvider"
            android:authorities="com.iips.launcher.config"
            android:exported="true"
            android:readPermission="com.iips.launcher.permission.READ_SHARED_CONFIG" />

        <!-- Disable default WorkManager initializer for on-demand initialization -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="\${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge"
            tools:replace="android:authorities">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Secure Wi-Fi Setup Screen -->
        <activity
            android:name=".ui.WifiSetupActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Dotroid.NoActionBar"
            android:excludeFromRecents="true"
            android:exported="false" />

        <!-- Kiosk Lock Screen Activity -->
        <activity
            android:name=".ui.KioskLockActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Dotroid.FullScreen"
            android:excludeFromRecents="true"
            android:exported="false" />

        <!-- Launcher Pairing View for Guard Manager -->
        <activity
            android:name=".ui.LauncherPairingActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Dotroid.NoActionBar"
            android:excludeFromRecents="true"
            android:exported="false" />

        <!-- App Pocket Activity -->
        <activity
            android:name=".pocket.ui.AppPocketActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Dotroid.NoActionBar"
            android:excludeFromRecents="true"
            android:exported="false" />
    </application>

</manifest>

