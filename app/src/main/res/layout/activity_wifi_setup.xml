<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:padding="24dp"
    tools:context=".ui.WifiSetupActivity">

    <!-- Header -->
    <ImageView
        android:id="@+id/wifi_logo"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@drawable/ic_dotroid_premium_branding"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/wifi_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Wi-Fi Networks"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary"
        android:layout_marginStart="16dp"
        app:layout_constraintTop_toTopOf="@id/wifi_logo"
        app:layout_constraintBottom_toBottomOf="@id/wifi_logo"
        app:layout_constraintStart_toEndOf="@id/wifi_logo" />

    <ProgressBar
        android:id="@+id/wifi_scan_progress"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/wifi_title"
        app:layout_constraintBottom_toBottomOf="@id/wifi_title"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- List of Networks -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_wifi_networks"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/wifi_logo"
        app:layout_constraintBottom_toTopOf="@id/btn_wifi_back"
        tools:listitem="@layout/item_wifi_network" />

    <!-- Footer Actions -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_wifi_back"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="56dp"
        android:text="Back"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_wifi_refresh"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="56dp"
        android:text="Scan"
        app:icon="@android:drawable/ic_menu_rotate"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
